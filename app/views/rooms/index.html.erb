<div class="card login-card">
  <div class="card-body">
    <h4 class="card-title"> Salas de Reunião </h4>
    <span>Clique em detalhes para conferir dias e horários disponíveis para reuniões.</span><br>
      <br><ul class="list-group">
        <% @rooms.each do |room| %>

            <li class="list-group-item">
              <h5>
                <%= room.name %>
                <div class=float-right>
                  <%= link_to 'DETALHES', room, class: "btn btn-primary" %>
                  <%= link_to 'DELETAR', room, method: :delete, data: { confirm: 'Tem certeza que deseja deletar esta Sala de Reunião?' }, class: "btn btn-danger" %>
                </div>
              </h5>
            </li>

        <% end %>
      </ul><br>

    <%= link_to 'CRIAR SALA DE REUNIÃO', new_room_path, class: "btn btn-success", role: "Button" %>
  </div>
</div>

